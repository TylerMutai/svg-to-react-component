{
  "name": "svg-to-component",
  "version": "1.0.0",
  "description": "Converts svg images to react components that are props aware",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "tsc && node --experimental-specifier-resolution=node ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Baliach Bali",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.2.1",
    "ts-node": "^10.9.1",
    "zip-a-folder": "^1.1.5"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
