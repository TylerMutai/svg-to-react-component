import IconBaseProps from "./iconBaseProps";
import React from "react";

/**
 * Defines the type of Icon.
 * @typedef {(props: IconBaseProps) => React.JSX.Element} IconType
 */

export default IconType;
